export enum UserRole {
  USER = 'utilisateur',
  CHEF = 'chef',
  ADMIN = 'admin',
}

export enum OrderStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in progress',
  READY = 'ready',
  SERVED = 'served',
}
